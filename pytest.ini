[pytest]
testpaths = tests
python_files = test_*.py

# Browser tests need Chromium and take ~a minute; the fast tiers take seconds.
# Default run excludes them. Run the full input matrix with:  pytest -m browser
addopts = -m "not browser" -q
markers =
    browser: requires Playwright + Chromium (deselected by default)
